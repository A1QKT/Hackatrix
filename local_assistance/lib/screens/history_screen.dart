import 'package:flutter/material.dart';

class HistoryScreen extends StatelessWidget {
  static String routName = '/history-screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('History'),
      ),
    );
  }
}
